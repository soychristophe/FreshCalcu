# ─── FreshWays Pro — Environment Variables ───────────────────────────────────
# Copy this file to .env.local and fill in the real values.
# NEVER commit .env.local to version control.

# Cloudflare Worker base URL (no trailing slash)
VITE_API_BASE=https://freshways-api.soychristophe.workers.dev

# PIN required to delete a product (was hardcoded as '1986')
VITE_DELETE_PIN=1986
