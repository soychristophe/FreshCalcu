# Styles

Copy your existing CSS files here verbatim:

```
src/styles/styles.css          ← was styles.css
src/styles/products-panel.css  ← was products-panel.css
```

No changes to the CSS are required for the migration.
Vite will bundle and hash them automatically on build.
